{% macro button(url, label, style, target=false) %}
    <a href="{{ url }}" {% if target == '_blank'%} target="_blank" rel="nofollow" {% endif %} class="btn {{ style }}">
        <span class="font-medium text-2xl leading-none">{{ label }}</span>
    </a>

{% endmacro %}