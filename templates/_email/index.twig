<!DOCTYPE html>
<html>
    <body>
        <div style="margin-top: 30px;"></div>
        <p>
        {% if globalAkCompany.fieldaKLogoHeader|length > 0 %}
            <img src={{ globalAkCompany.fieldaKLogoHeader.collect().first().url }}
                alt="Angelo Keirsebilck Logo"
                width="150" />
            <br />
            <br />
        {% endif %}
            
        </p>

        {{ contentHtml }}
    </body>
</html>
